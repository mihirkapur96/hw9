// Using module "circle.js"
const circle = require ("./circle.js");

// Using exported functions
console.log(`The circumference of a circle of radius 3 is ${circle.circumference(3)}`);
console.log(`It's area is ${circle.area(3)}`);
