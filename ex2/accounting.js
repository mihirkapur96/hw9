module.exports = class Accounting {
    constructor (name) {
        this.name = name;
        this.credit = credit; 
    }
    owner() {
        return `${this.name} , balance: ${this.credit}`;
    }
};